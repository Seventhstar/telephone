<%= form_for(@phone) do |f| %>
  <% if @phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone.errors.count, "error") %> prohibited this phone from being saved:</h2>

      <ul>
      <% @phone.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field fio">
 <table class="fio">
 <tr><td>
   <table class="table table-striped">
    <tr><td><%= f.label "ФИО:" %></td>
    <th><%= f.label @phone.name %></th> </tr>

    <tr><td><%= f.label "Номер телефона:" %></td>
      <th><%= @phone.number %></th></tr>
    <tr><td>    <%= f.label "Кабинет:" %></td>
          <td><%= @phone.room %></td>
      </tr>
  <tr><td>
    <%= f.label "Отдел:" %></td>
    <td><%= @phone.otdel_name %></td>
  <tr><td>
    <%= f.label "Должность:" %></td>
    <td><%= @phone.position_name %></td>
  <tr><td>
    <%= f.label "IP:" %><br /></td>
    <td><%= @phone.IP %></td>   
  <tr><td>
    <%= f.label "Имя компьютера:" %></td>
    <td><%= @phone.CompName %></td>
   </table>
  </div>
 
  </td>
   <td><div class="bs-docs-example">
   <div class="bs-docs-example_after">
	   <%= f.label "Обращаться по вопросам:" %><br />
   		</div>   
      <%= raw word_wrap(@phone.Duties).gsub("\n", "</br>") if @phone.Duties %>
     </div>
      <td>
  </table>
  </br>

<% end %>

<% if current_user && admin? %>
<%= link_to 'Изменить', edit_phone_path(@phone), :class=>"button" %> 
<% end -%>
<%= link_to 'Назад', phones_path, :class=>"button" %>
