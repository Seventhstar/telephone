<%= form_for(@basis) do |f| %>
  <% if @basis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@basis.errors.count, "error") %> prohibited this basis from being saved:</h2>

      <ul>
      <% @basis.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table>
  <tr><td><div class="field">
    <%= f.label t(:name) %><br>
    <%= f.text_field :name %>
  </div></td>
  
  <td>
    <div class="field">
    <%= f.label t(:img_name) %><br>
    <%= f.text_field :img_name %>
  </div>
  </td>

  <td><div class="field">
    <%= f.label t(:maintenance) %><br>
    <%= f.check_box :maintenance %>
  </div></td>
  </tr>

  <tr><td colspan="3"><div class="field">
    <%= f.label t(:service_text) %><br>
    <%= f.text_area :service_text, :size => "100x5" %>
  </div></td></tr>
  
  <tr><td>
  <div class="actions">
    <%= f.submit nil, :class => "button"%>
    <%= link_to t('Back'), bases_path, :class => "button" %>
  </div>

  </td></tr>
  </table>

<% end %>


