<%= form_for(@group_mag) do |f| %>
  <% if @group_mag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_mag.errors.count, "error") %> prohibited this group_mag from being saved:</h2>

      <ul>
      <% @group_mag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
